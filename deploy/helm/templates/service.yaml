apiVersion: v1
kind: Service
metadata:
  name: {{ include "ems-mvp.fullname" . }}-service
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "ems-mvp.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}